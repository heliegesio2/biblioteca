apiVersion: v1
kind: Service
metadata:
  name: {{ include "biblioteca.fullname" . }}
  labels:
    {{- include "biblioteca.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "biblioteca.selectorLabels" . | nindent 4 }}
