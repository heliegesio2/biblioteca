{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "biblioteca.serviceAccountName" . }}
  labels:
    {{- include "biblioteca.labels" . | nindent 4 }}
{{- end }}
